name: 'task'
description: 'Runs tasks'
inputs:
  task-path:
    description: 'Which task to run'
    required: true
    default: ''

runs:
  using: "composite"
  steps:
    - name: Run
      run: |
        bash tasks/task.sh tasks/${{ inputs.task-path }}
      shell: bash

